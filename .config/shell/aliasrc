#!/bin/sh

# use neovim for vim if present.
[ -x "$(command -v nvim)" ] && alias vim="nvim" vimdiff="nvim -d"

# Use $XINITRC variable if file exists.
[ -f "$XINITRC" ] && alias startx="startx $XINITRC"

# sudo not required for some system commands
for command in pacman mount umount su poweroff reboot ; do
	alias $command="sudo $command"
done; unset command

alias \
  ip='ip -color=auto' \
  cat='bat --theme="Visual Studio Dark+"' \
	grep="rg --color=auto" \
	diff="diff --color=auto" 

alias \
	sudo='sudo ' \
	cp="cp -iv" \
	mv="mv -iv" \
	rm='echo "This is not the command you are looking for."; false' \
	mkdir="mkdir -pv" \
	ffmpeg="ffmpeg -hide_banner" \
	wget="wget --hsts-file=$XDG_CACHE_HOME/wget-hsts" \
	ls="ls -ahN --color=auto --group-directories-first" \
  lf="lfub" \
  ka="killall" \
  newsboat="newsboat && cat ~/.cache/wal/sequences" \
	config="/usr/bin/git --git-dir=$HOME/.dots/ --work-tree=$HOME" \
  ssh="ssh -i $XDG_CONFIG_HOME/ssh/id_ed25519 -o UserKnownHostsFile=$XDG_DATA_HOME/ssh/known_hosts"
	purge="pacman -Qtdq | pacman -Rns -" \
  upgrade="paru; flatpak update"

alias \
  g="git " \
  gst="git status" \
  gaa="git add " \
  gcm="git commit -m " \
  gpu="git push origin " \
  gcb="git checkout -b " \
  gsw="git switch " \
  gpl="git pull origin " \
  glog="git log --graph --pretty='%Cred%h%Creset -%C(auto)%d%Creset %s %Cgreen(%ar) %C(bold blue)<%an>%Creset'" 

